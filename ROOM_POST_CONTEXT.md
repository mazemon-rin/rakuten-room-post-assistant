# ROOM投稿専用コンテキスト

ROOM投稿時に必要な最小情報だけを扱う。全仕様や履歴を商品ごとに再読込しない。

## 対象商品の確認

1件につき、次の項目だけを確認する。

- `itemCode`
- 商品名
- ショップ名
- 商品ページURL
- 保存済み`introText`
- 保存済み`hashTags`
- `postStatus`
- `postedAt`

商品名だけで同一商品とは判断しない。`itemCode`を対象商品の識別子とする。

## 重複防止

- 投稿履歴に同じ`itemCode`がないか完全一致で確認する。
- 同じ`itemCode`が投稿済み、または`postedAt`ありなら再投稿しない。
- 商品名、ショップ名、URLの一部だけによる曖昧一致を使わない。
- 画面に`itemCode`が直接表示されなくても、候補データの`candidate.itemCode`、`candidate.product.itemCode`、商品ページの表示情報を順に照合する。変換や推測はしない。
- 内部データと商品ページの照合後も一意に決まらない場合だけ停止して報告する。

## ROOM投稿

1. 対象商品の商品ページを開く。
2. 商品ページの商品名とショップ名を候補カードと照合する。
3. 商品ページ上に表示された「ROOMに投稿」を使用する。URLを推測・生成しない。
4. ROOM画面で商品名とショップ名を再照合する。
5. 保存済みの`introText`があれば使用する。空欄なら、商品ページで確認できた商品名・特徴・用途だけから未使用者向けの紹介文を作成し、候補へ保存して再確認する。確認できない価格・割引・期限・使用感は断定しない。
6. 保存済み`hashTags`が不足・空欄・商品名の不自然な分割なら、確認済みの商品カテゴリー・特徴・メーカー・用途から自然な5〜10個程度へ修正し、候補へ保存して再確認する。
7. 紹介文とハッシュタグの合計が500文字を超える場合は、事実情報を維持して紹介文を短く調整し、再確認する。
8. ユーザーが完了ボタンを押してよいと明示している場合だけ、ROOMの「完了」を押す。許可がない場合は入力確認で停止する。

## 完了確認

ROOMの完了後、同じ`itemCode`について次を順番に確認する。

1. ROOM側で投稿完了したこと。
2. アプリ側で投稿済みになったこと。
3. `postedAt`が保存されたこと。
4. 投稿履歴へ保存されたこと。

4項目を確認できるまで次の商品へ進まない。次の商品へ持ち越す結果は、完了した商品の`itemCode`、投稿成功または失敗、`postedAt`だけにする。

## 禁止

- `itemCode`からROOM URLを推測・生成・補完しない。
- Xへ投稿しない。
- Threadsへ投稿しない。
- 商品を勝手に変更しない。
- `itemCode`が一致しない商品を投稿しない。
- 投稿済み商品を再投稿しない。
- ROOM投稿完了の確認前にアプリを投稿済みにしない。

## ROOMだけの処理で読まない情報

ROOM投稿だけを依頼された場合、次の情報は参照しない。

- ランキング仕様
- 商品選定スコア
- トレンド検索
- 売上CSV
- 売上分析
- Threads生成仕様
- X生成仕様
- SNS成果型
- アフィリエイトURL
- コレクション推奨
- Git操作
- Deploy手順

候補画面に表示されてもROOM投稿の判断材料にしない。

## 自動復旧と停止の境界

次は不足していても、同じChromeプロフィール内で安全に取得・生成・確認してから続ける。

- ROOM固定タブがない：ROOMの通常ページを同じプロフィールで開き、固定入力先として準備する。商品固有URLは推測せず、商品ページ上の「ROOMに投稿」から進む。
- 紹介文が空欄：確認済み商品情報から生成・保存・再確認する。
- ハッシュタグが空欄または不自然：確認済み情報から生成・修正・保存・再確認する。
- `itemCode`が画面にない：候補内部データと商品ページ表示を照合する。
- 500文字超過：事実を変えずに紹介文を短くして再確認する。

次は自動解決せず停止する。

- `itemCode`が複数候補で一意に決まらない。
- 商品ページまたはROOM画面の商品が候補と一致しない。
- 投稿履歴に同じ`itemCode`がある。
- 商品情報が不足し、安全な紹介文・タグを作れない。
- 楽天またはROOM側で商品ページ、入力欄、完了操作ができない。

## 連続投稿

複数商品は、次の短い単位で逐次処理する。

`商品1の確認 → ROOM投稿 → 完了確認 → 結果だけ保持 → 商品2`

前の商品について、長い画面状態・操作ログ・説明全文を次の商品へ持ち越さない。
