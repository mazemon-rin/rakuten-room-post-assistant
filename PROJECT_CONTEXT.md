# PROJECT_CONTEXT

## Purpose

楽天ROOMの商品候補、紹介文プロンプト、投稿候補、投稿履歴を管理する静的Webアプリ。

## Current Status

Maintenance。HTML/CSS/JavaScript、楽天API、localStorage、JSONバックアップ・復元。自動投稿は行わない。

## Current Version

README記載のVersionを作業開始時に確認する。

## Stable State

商品検索、プロンプト生成、候補保存、投稿履歴、CSV、JSONバックアップを利用者操作で行う。

## Important Constraints

- 楽天ROOMへの自動投稿・自動いいね・自動フォローを追加しない。
- API認証情報を公開コードへ直書きしない。
- 楽天公式クレジット表示を削除・改変しない。

## ROOM投稿運用ルール

- 利用者が「ROOM投稿N件」と指定した場合、指定件数を1商品ずつ順番に処理する。
- 各商品は、商品情報だけを使って紹介文とハッシュタグを自動作成し、アプリへ保存してからROOM操作へ進む。
- 保存、商品一致確認、ROOM入力、「完了」押下、投稿済み記録を1商品ごとに完了してから次の商品へ進む。
- ROOM URLは商品情報から推測生成せず、楽天商品ページ上の「ROOMに投稿」を通常操作する。
- いいね、フォロー、コメント、購入などROOM投稿以外の操作は行わない。

## 正式なROOM→X→Threads運用

正式な工程は `商品選定 → CodexでROOM紹介文＋ハッシュタグ作成 → アプリへ反映 → ROOM投稿 → ROOM投稿完了確認 → SNS展開対象だけ確定ROOM個別URL取得 → アプリでURL入力・「登録完了」 → ROOM投稿済み・投稿履歴・postedAt・URLを同時保存 → Codexが続けてX・Threads文章作成 → Codexがアプリへ直接反映 → 内容確認待ち → X投稿 → Threads投稿 → 実投稿が完了した媒体だけSNSを投稿済みにする` とする。ROOMだけに投稿する商品はROOM個別URLを登録せず、SNS文章を作成しない。CodexがChromeを操作できる環境を正式経路とし、ChatGPT Workは利用可能な場合の選択肢であって必須条件ではない。アプリは外部AI APIやCodex APIへ接続せず、Codexが既存のChrome操作と結果反映欄を使う。

- ROOM投稿後のSNS生成では、保存済みの`introText`と`hashTags`を再生成せず、XとThreadsだけを生成する。
- 「登録完了」はSNS展開対象のX・Threads文章作成へ続けるトリガーとして扱う。Codex運用中は「X・Threads文章生成準備完了」で停止せず、本文作成・アプリ反映・検証後の人間確認待ちまで続ける。
- UIの「CodexでX・Threads作成」は補助・再実行用であり、通常の日次運用ではURL登録完了後に人間が改めて押す必要はない。
- ROOM URLは確定した実URLだけを使い、`itemCode`等から推測・生成・短縮しない。入力だけでは登録完了にせず、アプリの「登録完了」で保存・検証する。
- 「登録完了」は実際のROOM投稿完了後にだけ行い、候補の`status` / `postStatus`を投稿済みにし、同一履歴を`postedAt`・確定URL付きで作成または更新する。既存の「投稿済みにする」を先に使った場合も、後のURL登録は履歴を重複作成せず同一履歴へURLを反映する。
- Xは`discovery`を基本とし、ROOM URL、`#PR`、ハッシュタグ、改行を含む全文140文字以内とする。Threadsは`problem`を基本とし、140文字制限は設けない。
- X・Threadsの「投稿済みにする」は、各外部SNSへの実投稿が完了した後にだけ行う。文章生成・一括反映・機能確認だけでは投稿済みにしない。
- SNS一括反映はX／Threadsの区切り形式を使い、`snsPosts.x.text`と`snsPosts.threads.text`だけを更新する。解析・検証失敗時は既存文章を保持する。
- URL未登録時はURLを捏造せず、SNS本文に「ROOM個別URL未設定」と書かない。未使用商品の使用体験、未確認の価格・クーポン・ポイント・セール情報も捏造・断定しない。

## Threads限定投稿

- 候補管理画面は「投稿候補」タブ（ROOM自動運用）と「Threads投稿」タブ（Threads限定アフィリエイト投稿）に分かれる。初期表示は投稿候補で、タブ切り替えは保存データを変更しない。
- 商品カードの「投稿候補に保存」は従来どおりROOM投稿専用で、保存先は後方互換のため同じ`data.candidates`配列を使い、`destination: "room"`として扱う。
- 商品カードの「Threads投稿」は`destination: "threads_only"`の候補を作る。ROOM投稿候補、ROOM投稿待ち、ROOM紹介文・ハッシュタグ作成、ROOM個別URL、X投稿対象には追加しない。
- Threads限定候補は「Threads候補」→「文章作成待ち」→「確認待ち」→「投稿済み」を区別し、既存のThreads成果型Ver.1を基本モードとして使う。誰向けの表現は人間が確認・修正できる。
- Threads限定ではROOM URLを要求しない。楽天API設定にaffiliateIdがある場合だけ、APIが返したaffiliateUrlを商品・候補へ保存し、成果型文章の本文またはTHREADS_REPLYへ完全一致で使用する。affiliateId未設定・affiliateUrl未取得時はURLを推測・生成・代用・短縮せず、成果リンク付き完成状態とは扱わない。itemUrlやROOM URLをaffiliateUrlの代用にしない。
- Threads限定の文章反映はCodex＋Chrome経路で行えるが、外部Threadsへの実投稿は自動化せず、人間が投稿完了後に「投稿済みにする」を実行する。
- Threads投稿タブには「お得商品検索」を置き、楽天商品検索APIの割引表現を検索候補として表示する。検索ヒットだけでは割引確定にせず、`discountRate` / `rateConfirmed` / `discountRateType` / `couponDeadline` / `deadlineConfirmed` / `couponSource` / `couponCheckedAt`を確認状態とともに保存する。50%以上は`rateConfirmed === true`かつ`discountRate >= 50`かつ`discountRateType === "exact"`だけを正式対象とする。
- お得商品検索から保存したThreads限定候補は`performance_v1`の標準投稿方式を「本文＋返信URL」とし、親本文には原則URLを入れず、取得済みaffiliateUrlを`THREADS_REPLY`へ完全一致で1回反映する。RaCouponは直接取得せず、公式ページを開く補助にとどめる。
- `performance_v1`の「誰向け」は、広い属性ではなく、商品情報から合理的に導ける具体的な利用場面・小さな困りごとへ絞る。手動入力があれば優先し、なければ商品名・説明から自動提案する。本文＋返信URL方式では親投稿にURLを入れず、返信導線を置き、確認済みaffiliateUrlと`#PR`を`THREADS_REPLY`へ反映する。
- `destination`がない既存候補は従来どおりROOM候補として復元する。JSONバックアップ・復元では新しい区分を保持する。

## 商品選定の基本方針

### 対象カテゴリー

ランキング取得・商品選定の対象は、家電、パソコン・周辺機器、日用品・生活雑貨、キッチン用品・食器・調理器具、食品、美容・コスメ・香水、インテリア・寝具・収納、スポーツ・アウトドア、ベビー・キッズ・マタニティ、ペット・ペットグッズの10ジャンルとする。

ランキングは投稿商品を決める根拠ではなく、候補を発見する入口として扱う。投稿候補は、クリックされる理由と購入されやすい条件の両方を確認して総合的に判断する。

### 選定の順番

1. 楽天市場側の購入材料を確認する（ランキング、レビュー評価・件数、価格、セール、クーポン、ポイント還元、送料無料など）。
2. 「誰の、どんな悩みに、どのように役立つか」を具体化する。日常の小さなストレス、収納、家事、仕事、通勤、旅行、防災、美容などの利用場面に結び付けやすい商品を優先する。
3. 価格、セール、クーポン、ポイント、送料無料、レビューなど、クリック後に購入を検討しやすい条件を確認する。
4. クリックされやすさと購入されやすさを合わせて投稿候補を決める。

「人気」「ランキング1位」「かわいい」だけでは優先理由にしない。商品名や説明から対象者と悩みを合理的に説明できない商品は、ランキング上位でも優先順位を下げる。

初期検証では1,000〜3,000円前後の商品を優先するが、悩みとの関連性や購入条件が強い商品を価格だけで除外しない。

### 将来の評価スコア案（未実装・検証中）

将来スコア化する場合は、次の100点案を出発点とする。固定仕様ではなく、実績データで見直す。

- 悩みの明確さ：25点
- 楽天市場での売れやすさ：25点
- 価格の買いやすさ：20点
- セール・クーポン・ポイント：15点
- レビュー評価・件数：10点
- ランキング順位：5点

70点以上は優先候補、50〜69点は保留・条件確認、49点以下は原則として優先しない。ただし、この閾値も検証段階である。

### 紹介文生成との連携

商品選定時に整理した `targetUser`、`problem`、`mainBenefit`、`usageScene`、`saleReason` は、紹介文生成へ渡す中間情報として扱う。理想の流れは、商品取得、選定評価、中間情報の構造化、候補決定、紹介文生成、投稿キューの順番とする。

現時点では商品選定スコアやクリック数・売上による自動選定は実装しない。将来、投稿日、順位、価格、セール、クーポン、ポイント、レビュー、スコア、クリック数、成果、投稿タイプ、使用済み・未使用などを記録し、実績に基づいて重みを調整する。

## 商品信頼性チェック

商品選定の前に、取得できる商品情報から信頼性を確認する。判定は断定ではなく、次の4段階で管理する。

- 通常投稿候補
- 要確認
- 注意喚起候補
- 投稿対象外

メーカー、型番、商品識別情報、説明の整合性、スペックと価格の組み合わせ、ショップ、レビュー情報を確認する。レビュー評価や件数だけで通常判定せず、SSD、USBメモリ、SDカード、モバイルバッテリー、充電器などは容量・性能・保証を重点確認する。「偽物」「詐欺」「危険商品」などは確認できない限り断定しない。

候補には `trustStatus`、`trustScore`、構造化された `trustReasons`（`code`、`label`、`detail`、`severity`）、`manufacturer`、`modelNumber`、`specWarnings`、`priceWarning`、`descriptionWarnings`、`needsManualReview`、`warningContentCandidate`、`alternativeProductCandidate` を保持する。既存データに項目がない場合は表示時に安全な初期値を補完する。

## ROOMコレクション運用

注意喚起候補は、`🔍 買う前に確認したい商品` コレクションを推奨する。これは偽物・危険商品の断定や購入禁止を意味せず、価格・スペック・メーカー・型番・保証などを購入前に確認したい商品を整理するためのものとする。

運用は `注意喚起候補 → コレクション推奨 → 人間が確認 → 必要に応じて選択コレクションを変更 → 手動投稿` の順で行う。要確認商品は自動でコレクションを決定せず、投稿対象外は原則コレクションへ追加しない。ROOM側のコレクション作成・登録や注意喚起商品の自動投稿は行わない。

候補・履歴には必要に応じて `postType`、`recommendedCollection`、`selectedCollection`、`collectionReason`、`collectionStatus` を保持する。コレクション設定はコード内の一覧で管理し、将来追加できる構造とする。

## 商品選定アルゴリズム Ver.1.0

ランキングは投稿商品の自動採用ではなく、候補発見用の入口として扱う。商品信頼性チェックを先に行い、通常投稿候補だけを次の100点評価で通常商品スコアに入れる。要確認・注意喚起候補・投稿対象外は通常商品ランキングから分離する。

- クリック期待度：30点
- 悩みとの一致：20点
- 購入材料：15点
- 信頼性：15点
- ROOMとの関連性：10点
- 季節・セール性：10点

候補には `selectionScore`、`selectionBreakdown`、各項目のスコア、`targetUser`、`problem`、`mainBenefit`、`usageScene`、`clickReason`、`purchaseReasons`、`selectionReasons`、`selectionWarnings`、`selectionStatus`、`scoreVersion` を保存する。現在の配点は初期仮説・検証用であり、固定された正解ではない。将来、クリック・誘導・成果・商品タイプなどの実績データとの相関を確認して調整する。

## Known Issues

- 公開環境、APIキーの保存方式、バックアップ復元は要確認。
- 31〜50位は実APIで取得・表示確認済み。ただし、実APIレスポンスにおける各商品の `rank` / `apiRank` / `sourceRank` の個別値は直接確認できていない。

## Recent Work

- 次: 対象機能のREADMEとコードを照合する。

## Next

API、保存、プロンプト、投稿履歴のいずれかを限定して確認する。

## Verification

- JavaScript構文確認
- サンプル商品で検索
- 保存、CSV、JSONバックアップ・復元
- API未設定時の表示

## Human Check

API認証、投稿内容、PR表記、価格・在庫、公開可否。
